{
  "name": "orbit-remote-signaling",
  "version": "1.0.0",
  "private": true,
  "description": "WebRTC signaling server for Orbit Remote — device registry, session pairing, signal relay, connection logging.",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
